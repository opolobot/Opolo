# When using the bot yourself, you need to rename this file to config.yml
token: "keep this a secret"
errorChannel: "make sure to fill this in, as their is no empty nil value fallback right now"
prefix: "w!"
status: "I'm just a beverage"
